# P3 — Repo, PWA, Results Flow, Teacher View, Deploy

**Mission:** the rails everyone runs on. You go first (minutes 0–10 block the
whole team), you own the deploy, and you own the PWA — our airplane-mode demo
moment.

**You own:** `src/app/api/results/**`, `src/app/teacher/**`, `src/lib/store.ts`,
`src/lib/resultSync.ts`, `src/app/layout.tsx`, `src/app/manifest.ts`,
`public/sw.js`, `public/icons/**`, `src/components/SWRegister.tsx`, Vercel.
Sole authority over `src/lib/types.ts` changes (team ping first).

## Agent split

- **Claude Code (main):** scaffold → PWA → results API, in order below.
- **Codex (parallel, fire after scaffold):** teacher page + seed data per spec.

## Checklist (time-boxed)

### 0:00–0:10 — Scaffold (TEAM IS BLOCKED ON THIS)
- [ ] `create-next-app` (TS, App Router, Tailwind, src dir) into the repo root
- [ ] Commit `src/lib/types.ts` (contracts from TASKS.md, verbatim) +
      `src/lib/mockItems.ts` (10 hand-written items, all 3 formats, no audio —
      P1 needs them at minute 10; 15 minutes of hand-writing beats waiting on P2)
- [ ] `.gitignore` has `.env.local`; push; post "repo up" in team chat
- [ ] Vercel: import repo, set `OPENAI_API_KEY`, first deploy — broken is fine

### 0:10–0:40 — PWA (in scope, keep it this small)
- [ ] `src/app/manifest.ts`: name "Basa Buddy" (or team name), `display:
      "standalone"`, portrait, theme color, icons 192 + 512
- [ ] Icons: `magick -size 512x512 xc:"#f59e0b" -gravity center -pointsize 320
      -annotate 0 "🦉" public/icons/icon-512.png` (+192) — or any 2-min variant
- [ ] `public/sw.js`, handwritten (~50 lines, no plugins):
  - install: precache `/`, `/child`, icons
  - fetch: **cache-first** for `/audio/**`, `/icons/**`, `/_next/static/**`
    (immutable); **network-first with cache fallback** for navigations
  - version string constant at top; bump = old caches deleted on activate
- [ ] `<SWRegister>` client component in `layout.tsx` (register on load, prod only)
- [ ] `src/lib/resultSync.ts`: `syncResult(result)` → POST `/api/results`; on
      failure write to `localStorage["pending-results"]` and retry on next load —
      this is what makes airplane-mode finish survivable
- [ ] **Acceptance test (do it now, not at 1:30):** deployed URL on your phone →
      load `/child` once → airplane mode → reload → page + mock item audio still
      work. This test IS the PWA task
- [ ] After P2's audio lands (~1:10): confirm mp3s are SW-cached after one
      session (cache-first handles it; verify one URL in DevTools)

### 0:40–1:10 — Results API + teacher view
- [ ] `src/lib/store.ts`: module-level `Map<string, SessionResult>` +
      `globalThis` guard (survives dev hot-reload; fine on Vercel for demo)
- [ ] `POST /api/results` (idempotent on `studentName`), `GET /api/results`
- [ ] Recompute θ server-side from `responses` via P2's `estimate()` — server
      value wins (mock until engine lands, `// SWAP:`)
- [ ] Codex meanwhile: `/teacher` page — seed 12 fake students (spread of θ,
      bands, 2 red "Needs support" flags), table: name, band chip
      (color-coded), θ bar, vocab/comprehension split, flag column; live GET
      results merge on top of seeds; header shows class name + count flagged
- [ ] Auto-refresh: 5 s polling. No websockets

### 1:10–1:30 — Integration + freeze
- [ ] P1's real session lands in the teacher table alongside seeds — the
      three-audience demo path works on the deployed URL
- [ ] Redeploy; re-run airplane-mode test with real audio; freeze

## Definition of done
Installable PWA (Android "Add to Home Screen" produces a standalone app);
airplane-mode session completes and syncs on reconnect; live result appears in
the teacher table on the deployed URL.

## Do NOT
Add a DB, auth, next-pwa/Serwist, background-sync API (localStorage retry is
enough), or let anyone else touch Vercel settings.
